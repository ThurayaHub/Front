# Fonts

The Figma design uses Tajawal. The Regular (400), Medium (500), and Bold (700)
font files are registered in `pubspec.yaml` and applied through `AppTheme`.

`OFL.txt` contains the font license.
